# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
*~

# Claude Code local settings (keep launch.json for the run recipe)
.claude/settings.local.json

# Local build / dependency dirs (none required, but ignore if added later)
node_modules/
dist/

# Scratch / temporary import fixtures
_import_test.json
*.tmp
